services:
  postgres-db:
    image: postgres:16.1
    container_name: shareitPostgre
    environment:
      POSTGRES_DB: shareit
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - ./db/:/var/lib/postgresql/data
    restart: always
